version: 1.0
build:
  image: python:3.11-slim  # Use the same base image as our Dockerfile
run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  variables:
    PORT: "8080" # Set the PORT variable for consistency